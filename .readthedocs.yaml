# Read the Docs configuration file
# EasyGo Solutions Automated Build Config
version: 2

build:
  os: ubuntu-22.04
  apt_packages:
    - gettext
  tools:
    python: '3.11'

sphinx:
  configuration: conf.py

python:
  install:
    - requirements: requirements.txt
